# CSP product documentation (SEA01253) — HedEx package

## What is in the handover folder

The file `CSP_V600R003C62_01_en_SEA01253.hdxi` under:

`…\handover document for IPCC\CSP\SEA01253-(For Customer)CSP Product Documentation-(V600R003C62_01)_HedEx\`

is a **HedEx index stub** (about 61 bytes). It contains only these lines:

- `Product=CSP`
- `Version=V600R003C62`
- `DocID=SEA01253`
- `DocVer=01`

It is **not** a self-contained HTML/PDF export. The full CSP V600R003C62 customer documentation is normally delivered as a HedEx library that HedEx resolves against its content database or additional media.

## How to get a readable export

1. Install **Huawei HedEx Lite** (or the HedEx version your organization uses for customer documentation).
2. Open the `.hdxi` file from the path above. If HedEx reports missing content, obtain the **complete SEA01253** delivery from Huawei or your vendor (same DocID/version) and register it in HedEx per vendor instructions.
3. From HedEx, use **Export** (HTML, CHM, or PDF, depending on HedEx build) for offline reading or internal wiki upload.

## Duplicate copy

The same `…\handover document for IPCC1\CSP\…\CSP_V600R003C62_01_en_SEA01253.hdxi` path may exist; treat it the same way.

## Related operational docs (plain text)

For architecture and operations that *are* in plain text in your tree, see the IPCC/DataStation notes (`task.txt`, `DSConfig.txt`, `vpn.txt`, `Решение отчета  1728.txt`, `tns for IVR.txt`) under `Инструкция по CSP`.
